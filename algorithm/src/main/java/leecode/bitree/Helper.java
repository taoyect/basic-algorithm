package leecode.bitree;

public class Helper {
    public TreeNode node;
    public boolean isFirstEntry;

    public Helper(TreeNode node, boolean isFirstEntry) {
        this.node = node;
        this.isFirstEntry = isFirstEntry;
    }
}
